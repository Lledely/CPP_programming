#include <ConsoleIO.h>
#include <Game.h>
#include <GameDictionary.h>

int main( int argc, char **argv, char **envp ) {

    GameDictionary a = {};
    // std::cout << a.get_random_word() << std::endl;

    return 0;
}